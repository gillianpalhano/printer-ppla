import { TDirections } from '../index.type'

export const Direction: { PORTRAIT: TDirections; REV_LAND: TDirections; REV_PORT: TDirections; LANDSCAP: TDirections } = {
  PORTRAIT: '1',
  REV_LAND: '2',
  REV_PORT: '3',
  LANDSCAP: '4',
};